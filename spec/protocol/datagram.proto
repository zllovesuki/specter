syntax = "proto3";
package protocol;

option optimize_for = SPEED;
option go_package = "specter/spec/protocol";

message Datagram {
    enum Type {
        UNKNOWN = 0;
        ALIVE = 1;
    }

    Type type = 1;
}